name: VTMK_CLI
version: "0.1"
about: VTMK Command Line tool
author: Kaniya Simeji <main@annko.tech>

args:
  - version:
      about: Print VTMK version
      short: v
      long: version
subcommands:
  - assoc:
      args:
        - directory:
            short: d
            long: dir

  - commit:
      args:
        - release:
            short: r
            long: release
  - name:
      args:
        - namechange:
            short: nc
            long: name-change

  - remote:
      args:
        - query:
            short: q
            long: query
        - add:
            long: add
        - remove:
            long: remove
  - remote-remove:

  - remote-add:
      args:
        - release:
            short: r
            long: release

  - search:

  - clone:
      args: 
        - release:
            short: r
            long: release

  - tag:
      args:
        - add:
            long: add
        - remove:
            long: remove
        - list:
            long: list
  
  - config:
      args:
        - global:
            short: g
            long: global 

  - group:
      args:
        - list:
            short: l
            long: list

